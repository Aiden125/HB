<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="styles.css">
    <title>가격비교</title>
</head>
<body>
{{>layout/header}}

<!-- 목록 출력 영역 -->
<table class="table table-horizontal table-bordered">
    <thead class="thead-strong">
    <tr>
        <th class="content-small-column">글번호</th>
        <th class="title-column">제목</th>
        <th class="content-normal-column">작성자</th>
        <th class="content-normal-column">날짜</th>
    </tr>
    </thead>

    <tbody id="tbody">
    {{#posts}} <!-- posts 리스트 순회 -->
    <tr>
        <td>{{id}}</td> <!-- posts 객체의 필드 사용 -->
        <td><a href="/posts/view/{{id}}">{{title}}</a></td>
        <td>{{author}}</td>
        <td>{{modifiedDate}}</td>
    </tr>
    {{/posts}}
    </tbody>

</table>

<a href="/posts/save" role="button" class="btn btn-primary right">글 등록</a>

</body>
</html>
